import { Place } from "@/infra/postgres/entities";
import { IPostGreAddRepository } from "@/infra/helpers/repos/place";

import { EntityRepository, Repository } from "typeorm";
import { PlaceDTO } from "@/data/contracts/dtos";

@EntityRepository(Place)
export class PostGreAddRepository extends Repository<Place> implements IPostGreAddRepository{
  async add(data:PlaceDTO):Promise<PlaceDTO>{
    const place = this.create(data);
    await this.save(place);

    return place
  }
}



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































