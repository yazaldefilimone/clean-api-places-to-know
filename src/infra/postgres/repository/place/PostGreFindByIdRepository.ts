import { Place } from "@/infra/postgres/entities";
import { IPostGreFindByIdRepository } from "@/infra/helpers/repos/place";

import { EntityRepository, Repository } from "typeorm";
import { PlaceDTO } from "@/data/contracts/dtos";

@EntityRepository(Place)
export class PostGreByIdRepository extends Repository<Place> implements IPostGreFindByIdRepository{

  async findById({ id }:PlaceDTO | any):Promise<PlaceDTO>{
    const place: PlaceDTO = await this.findOne({ id }) as any;

    return place 
  }
}



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































